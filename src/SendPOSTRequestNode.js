import React from 'react';

const SendPOSTRequestNode = ({ id }) => {
  return (
    <div style={{ background: '#fff', border: '1px solid #000', padding: '10px', borderRadius: '5px' }}>
      Send POST Request Node - {id}
    </div>
  );
};

export default SendPOSTRequestNode;
