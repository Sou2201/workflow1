import React from 'react';

const ConvertFormatNode = ({ id }) => {
  return (
    <div style={{ background: '#fff', border: '1px solid #000', padding: '10px', borderRadius: '5px' }}>
      Convert Format Node - {id}
    </div>
  );
};

export default ConvertFormatNode;
